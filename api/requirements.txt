# Runtime deps for the Vercel Python function. Deliberately NO torch and NO
# ultralytics - that pair is ~450-600 MB installed and blows the function size
# cap. Everything they were used for is reimplemented in _yolo_post.py.
#
# Measured cp312 manylinux_2_28 install size: 288 MB
#   cv2 + libs 136 | onnxruntime 58 | numpy + libs 76 | Pillow + libs 17 | rest 1
# Plus models/*.onnx (55 MB) => ~343 MB against Vercel's 500 MB uncompressed cap.
#
# Do NOT downgrade onnxruntime below 1.24: 1.22.x still declares sympy+mpmath
# (85 MB, only used by the offline symbolic-shape-inference tool) as hard
# dependencies, which pushes the bundle to ~422 MB for no benefit.
numpy==1.26.4
onnxruntime==1.28.0
opencv-python-headless==4.10.0.84
Pillow==10.4.0

